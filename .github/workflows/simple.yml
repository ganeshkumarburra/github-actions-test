name: Shell Commands
on: push
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: multi line script
        run: |
          npm --version
          node --version
      - name: python shell command
        run: |
          import platoform
          print(platform.processor())
        shell: python

  run-windows-command:
    runs-on: windows-latest
    steps:
      - name: Get PoerShell Directory
        run: Get-Location
      - name: Get shell directory
        run: pwd
        shell: bash
